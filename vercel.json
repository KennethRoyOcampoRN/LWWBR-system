{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "_comment": "Spec §3.1 / §11 M0: written now, not deployed until M7 launch. No cloud account exists yet.",
  "regions": ["sin1"],
  "builds": [
    { "src": "apps/api/src/serverless.ts", "use": "@vercel/node" },
    { "src": "apps/web/package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/apps/api/src/serverless.ts" },
    { "source": "/(.*)", "destination": "/apps/web/dist/$1" }
  ],
  "crons": [
    {
      "_comment": "Endpoint ships in M5 (spec §7.4) — this entry is inert until then; it is not deployed in M0.",
      "path": "/api/v1/jobs/amenity-overdue",
      "schedule": "*/15 * * * *"
    },
    {
      "_comment": "Endpoint ships in M6 (spec §8.3) — 8:00 AM PHT = 00:00 UTC. Not deployed in M0.",
      "path": "/api/v1/jobs/owner-digest",
      "schedule": "0 0 * * *"
    }
  ]
}
